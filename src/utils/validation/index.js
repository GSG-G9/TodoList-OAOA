const { loginSchema } = require('./loginSchema');
const { signupSchema } = require('./signupSchema');
const { addListSchema } = require('./addListSchema');

module.exports = {
  loginSchema,
  signupSchema,
  addListSchema,
};
