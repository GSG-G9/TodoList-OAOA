const { addList } = require('./addList');
const { deleteList } = require('./deleteList');
const { updateList } = require('./updateList');

module.exports = {
  addList,
  deleteList,
  updateList,
};
