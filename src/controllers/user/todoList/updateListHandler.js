const { verify } = require('../../../utils/validation');

process.env.SECRET_TOKEN;
const updateListHandler = (req, res, next) => {

};

module.exports = { updateListHandler };

// verify token => check token id DB => list id => newName
